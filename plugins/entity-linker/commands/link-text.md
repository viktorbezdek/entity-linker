---
description: Link entities in pasted text
argument-hint: "[--format markdown|xml|sidecar] [--type asr|email|markdown|html|plain]"
---

# /link-text

Resolves entity mentions in text you paste directly. Useful for quick one-off lookups without saving a file.

Paste your text after the command and press Enter. The `entity-linker` skill handles detection, preprocessing, and resolution. Output is printed to the conversation.
