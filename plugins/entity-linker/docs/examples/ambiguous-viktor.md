# Ambiguous Viktor Reference

This document contains a reference to "Viktor" without strong type cues,
which should trigger disambiguation rather than auto-linking.

The meeting notes mention Viktor but the context is neutral — no clear
person cues like "synced with" or "called". Viktor's role here is unclear.

This tests TS-003: the Disambiguation App or elicitation fallback.
