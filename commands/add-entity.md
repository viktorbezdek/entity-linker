---
description: Add a new entity to the catalog interactively
---

# /add-entity

Interactively creates a new entity in the catalog via the `catalog_create` MCP tool.

Prompts for: entity type, canonical name, aliases, and an optional disambiguation hint. The entity is created immediately (no staging). Phonetic and trigram indices are built automatically.

Uses the `entity-catalog-manage` skill.
